server:
  port: 9001

spring:
  application:
    name: agile-decision-engine-test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://{ db.ip }/{ dp.name }?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=UTF-8
    username: { username }
    password: { password }